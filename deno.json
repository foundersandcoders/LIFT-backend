{
  "tasks": {
    "dev": "deno run -A --watch main.ts",
    "dbGet": "deno run -A --watch queries/queryAll.ts",
    "dbSeed": "deno run -A --watch queries/seed.ts",
    "dbWipe": "deno run -A --watch queries/wipe.ts",
    "dbWrite": "deno run -A --watch queries/write.ts",
    "lint": "deno lint"
  },
  "imports": {
    "acorn": "jsr:@oak/acorn@^1.1.1",
    "env": "jsr:@std/dotenv",
    "neo4j": "npm:neo4j-driver@^5.27.0",
    "types": "./utils/interfaces.ts",
    "calc": "./utils/calc.ts",
    "router": "./routes/hub.ts"
  },
  "lint": {
    "rules": {
      "tags": ["recommended"],
      "include": ["ban-untagged-todo"],
      "exclude": ["no-unused-vars"]
    }
  }
}
