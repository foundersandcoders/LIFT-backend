{
  "tasks": {
    "dbSeed": "deno run -A --watch dbUtils/seed.ts",
    "dbWipe": "deno run -A --watch dbUtils/wipe.ts",
    "dbWrite": "deno run -A --watch dbUtils/write.ts",
    "dev": "deno run -A --watch main.ts",
    "lint": "deno lint"
  },
  "imports": {
    "acorn": "jsr:@oak/acorn@^1.1.1",
    "calc": "./utils/calc.ts",
    "neo4j": "npm:neo4j-driver@^5.27.0",
    "router": "./routes/hub.ts",
    "queries": "./queries/"
  },
  "lint": {
    "rules": {
      "tags": ["recommended"],
      "include": ["ban-untagged-todo"],
      "exclude": ["no-unused-vars"]
    }
  }
}
